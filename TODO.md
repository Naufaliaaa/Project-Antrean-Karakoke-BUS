# TODO - Edit Request Antrean (batas nomor 2 tidak boleh)

## Step 1
- [x] Pahami struktur antrean & field request di Firebase (`JS/form.js`, `JS/room.js`).
- [x] Cek halaman request (`form.html`, `CSS/form.css`).

## Step 2
- [ ] Tambah UI tombol/section “Edit Request” di `form.html`.
- [ ] Tambah styling di `CSS/form.css`.

## Step 3
- [ ] Implementasi logika di `JS/form.js` untuk menentukan posisi antrean user berdasarkan `order`.
- [ ] Disable tombol edit bila posisi user <= 2.

## Step 4
- [ ] Implementasi fungsi `updateSongRequest()` untuk update `name` & `videoId` di Firebase untuk request milik `deviceId`.
- [ ] Validasi YouTube embed saat edit.

## Step 5
- [ ] Pengujian: user nomor 1/2 tidak bisa edit, user nomor 3+ bisa edit dan display berubah.

